VITE_API_URL=http://localhost:3001

# Serve the API from the MSW handlers in src/mocks instead of a real server. The whole
# API is implemented now, so this defaults to off; set it to true to run the front end
# with no database and no server process.
VITE_USE_MOCKS=false
